# 🌙 Hop-In Express Client App - Onboarding Guide

**Version**: 1.0.0 (Alpha)
**Tech Stack**: React Native (Expo) + TypeScript + NativeWind

## 🚀 Welcome to the Luxury Digital Concierge

This codebase powers the customer-facing mobile application for Hop-In Express. It is designed with the "Midnight Vanta" aesthetic to provide a high-end shopping experience.

### 📂 Project Structure

We follow a **Feature-First Architecture** combined with Atomic Design principles for UI components.

```
Hop-in-express-clientapp/
├── app/                 # Expo Router (File-based routing)
│   ├── (tabs)/          # Main Tab Navigation (Home, Search, Bag, Profile)
│   ├── product/         # Product Detail Modals
│   └── _layout.tsx      # Root Layout & Theme Providers
├── src/
│   ├── components/
│   │   ├── ui/          # "Atoms" - Buttons, Cards, Inputs, Typography (Re-usable, dumb)
│   │   └── modules/     # "Molecules" - ProductCard, CartItem, HeroSection (Business logic)
│   ├── constants/       # Global Design Tokens (Colors, Fonts)
│   ├── hooks/           # Custom React Hooks (useCart, useAuth)
│   ├── lib/             # Utilities (Firebase, Stripe, Formatters)
│   ├── store/           # Global State (Zustand)
│   └── types/           # TypeScript Definitions
├── assets/              # Images, Fonts, Icons
└── ONBOARDING.md        # You are here
```

### 🛠️ Getting Started

1.  **Install Dependencies**: `npm install`
2.  **Start Development**: `npx expo start`
    *   Press `a` for Android Emulator
    *   Press `i` for iOS Simulator (Mac only)
    *   Press `w` for Web
    *   Scan QR code with Expo Go app on physical device.

### 🎨 Key Design Systems

*   **Midnight Vanta Theme**: All backgrounds are `#050505` or darker. No pure white backgrounds.
*   **Typography**:
    *   Headings: *Playfair Display* (Imported via `expo-font`)
    *   Body: *Inter* or System Sans
*   **Glassmorphism**: Use `BlurView` from `expo-blur` heavily for overlays.

---

### 📦 Version Control & Branching

*   `main`: Production-ready code.
*   `dev`: Integration branch.
*   `feat/feature-name`: For new features.

---
*Generated by Antigravity Agent*
